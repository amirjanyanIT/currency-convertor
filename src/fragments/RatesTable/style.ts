import styled from 'styled-components';

export const Container = styled.div`
    margin: 10px 0;
    .actions-container {
        display: flex;
        gap: 10px;
        margin-bottom: 10px;
        button {
            flex: 1;
        }
    }
`